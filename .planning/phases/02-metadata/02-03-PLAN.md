# Plan 02-03: Salvataggio copertina album + ImportUseCase

## Objective
Creare `AlbumArtSaver` che persiste la copertina embedded come PNG locale, e `ImportTrackUseCase` che orchestra l'intero flusso: pick URI → estrai metadati → salva copertina → inserisci nel DB.

## Context
- @.planning/phases/02-metadata/02-02-PLAN.md — `MetadataExtractor`
- @.planning/phases/01-foundation/01-03-PLAN.md — `TrackRepository`

## Tasks

### Task 1: `AlbumArtSaver`
**Type:** create
**File:** `app/src/main/java/com/example/player/domain/AlbumArtSaver.kt`

```kotlin
package com.example.player.domain

import android.content.Context
import android.graphics.BitmapFactory
import dagger.hilt.android.qualifiers.ApplicationContext
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext
import java.io.File
import java.util.UUID
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class AlbumArtSaver @Inject constructor(
    @ApplicationContext private val context: Context
) {
    private val artDir: File
        get() = File(context.filesDir, "album_art").also { it.mkdirs() }

    /**
     * Salva i byte della copertina come PNG nella directory privata dell'app.
     * Ritorna il path assoluto del file salvato, o null se i byte non sono validi.
     */
    suspend fun save(artBytes: ByteArray): String? = withContext(Dispatchers.IO) {
        val bitmap = BitmapFactory.decodeByteArray(artBytes, 0, artBytes.size) ?: return@withContext null
        val file = File(artDir, "${UUID.randomUUID()}.png")
        file.outputStream().use { out ->
            bitmap.compress(android.graphics.Bitmap.CompressFormat.PNG, 90, out)
        }
        file.absolutePath
    }
}
```

---

### Task 2: `ImportTrackUseCase`
**Type:** create
**File:** `app/src/main/java/com/example/player/domain/ImportTrackUseCase.kt`

```kotlin
package com.example.player.domain

import android.net.Uri
import com.example.player.data.repository.TrackRepository
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class ImportTrackUseCase @Inject constructor(
    private val metadataExtractor: MetadataExtractor,
    private val albumArtSaver: AlbumArtSaver,
    private val trackRepository: TrackRepository
) {
    /**
     * Importa una singola traccia dall'URI fornito.
     * Salta se l'URI è già presente nel DB.
     * Ritorna true se la traccia è stata inserita, false altrimenti.
     */
    suspend operator fun invoke(uri: Uri): Boolean = withContext(Dispatchers.IO) {
        // Evita duplicati
        val existing = trackRepository.getTrackByUri(uri.toString())
        if (existing != null) return@withContext false

        // Salva copertina se presente
        val artBytes = metadataExtractor.extractAlbumArt(uri)
        val artPath = artBytes?.let { albumArtSaver.save(it) }

        // Estrai metadati e inserisci
        val track = metadataExtractor.extract(uri, artPath) ?: return@withContext false
        trackRepository.insertTrack(track)
        true
    }

    /**
     * Importa più tracce in batch. Ritorna il numero di tracce inserite.
     */
    suspend fun importAll(uris: List<Uri>): Int = uris.count { invoke(it) }
}
```

**Done:** Use case completo con deduplicazione per URI.

---

## Verification
- [ ] `AlbumArtSaver` — salva in `context.filesDir/album_art/`, directory creata con `mkdirs()`
- [ ] `ImportTrackUseCase` — deduplicazione per URI, gestisce errori con ritorno booleano
- [ ] Tutto eseguito su `Dispatchers.IO`
