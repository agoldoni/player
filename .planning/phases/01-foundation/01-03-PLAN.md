# Plan 01-03: Repository + Hilt Modules

## Objective
Creare il `TrackRepository` e i moduli Hilt (`DatabaseModule`, `RepositoryModule`) per iniettare il database e la repository nell'app.

## Context
- @.planning/phases/01-foundation/01-02-PLAN.md
- Package base: `com.example.player`

## Tasks

### Task 1: `TrackRepository`
**Type:** create
**File:** `app/src/main/java/com/example/player/data/repository/TrackRepository.kt`

```kotlin
package com.example.player.data.repository

import com.example.player.data.local.dao.TrackDao
import com.example.player.data.local.entity.Track
import kotlinx.coroutines.flow.Flow
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class TrackRepository @Inject constructor(
    private val trackDao: TrackDao
) {
    fun getAllTracks(): Flow<List<Track>> = trackDao.getAllTracks()

    suspend fun insertTrack(track: Track) = trackDao.insertTrack(track)

    suspend fun insertTracks(tracks: List<Track>) = trackDao.insertTracks(tracks)

    suspend fun deleteTrack(track: Track) = trackDao.deleteTrack(track)

    suspend fun getTrackByUri(uri: String): Track? = trackDao.getTrackByUri(uri)

    suspend fun getTrackById(id: String): Track? = trackDao.getTrackById(id)
}
```

**Done:** Repository creata con tutte le operazioni del DAO.

---

### Task 2: `DatabaseModule`
**Type:** create
**File:** `app/src/main/java/com/example/player/di/DatabaseModule.kt`

```kotlin
package com.example.player.di

import android.content.Context
import androidx.room.Room
import com.example.player.data.local.PlayerDatabase
import com.example.player.data.local.dao.TrackDao
import dagger.Module
import dagger.Provides
import dagger.hilt.InstallIn
import dagger.hilt.android.qualifiers.ApplicationContext
import dagger.hilt.components.SingletonComponent
import javax.inject.Singleton

@Module
@InstallIn(SingletonComponent::class)
object DatabaseModule {

    @Provides
    @Singleton
    fun providePlayerDatabase(@ApplicationContext context: Context): PlayerDatabase =
        Room.databaseBuilder(
            context,
            PlayerDatabase::class.java,
            PlayerDatabase.DATABASE_NAME
        ).build()

    @Provides
    @Singleton
    fun provideTrackDao(db: PlayerDatabase): TrackDao = db.trackDao()
}
```

**Done:** Database e DAO esposti come singleton Hilt.

---

## Verification
- [ ] `TrackRepository` — annotata `@Singleton`, costruttore `@Inject`
- [ ] `DatabaseModule` — installato in `SingletonComponent`
- [ ] Tutti i `@Provides` presenti: `PlayerDatabase` e `TrackDao`
- [ ] Struttura package: `data/repository/`, `di/`
